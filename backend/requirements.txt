# FastAPI and ASGI Server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9  # PostgreSQL adapter
aiosqlite==0.19.0  # Async SQLite support

# Pydantic for data validation
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.1

# Redis
redis==5.0.1
hiredis==2.2.3  # Faster Redis protocol parser

# Celery for background tasks
celery==5.3.4

# WebSocket support
python-socketio==5.10.0
websockets==12.0

# HTTP client for external APIs
httpx==0.25.2
aiohttp==3.9.1

# AI/ML
anthropic>=0.40.0

# Email
sendgrid==6.11.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2  # For testing FastAPI

# Code Quality
black==23.11.0
isort==5.12.0
flake8==6.1.0
mypy==1.7.1

# Utilities
python-dotenv==1.0.0
pytz==2023.3
python-dateutil==2.8.2

# Existing dependencies (keep compatibility with Phase 1/2)
# These are already in the main requirements.txt, but included here for backend isolation
rich==13.7.0
click==8.1.7
schedule==1.2.0
reportlab==4.0.7
plotext==5.2.8
textual==0.50.0
